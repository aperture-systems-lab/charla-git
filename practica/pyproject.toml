[project]
name = "charla-git-practica"
version = "0.1.0"
description = "Parte práctica de la charla: resolver un reto de Deep-ML y aportarlo con un fork y un pull request."
requires-python = ">=3.13"
dependencies = [
    "numpy>=1.26",
]
